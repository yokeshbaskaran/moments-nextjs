const Box = () => {
  return (
    <div
      style={{
        background: "linear-gradient(to right,#4facfe,#b600fe)",
        padding: "3rem 1rem",
        textAlign: "left",
        color: "white",
      }}
    >
      <h2 className="mx-4 px-5 py-4 text-2xl font-medium rounded text-black bg-white fw-semibold fs-2">
        Login to Capture the moments and share it with the real world!
      </h2>
    </div>
  );
};

export default Box;
